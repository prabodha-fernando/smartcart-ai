meta {
  name: Delete wishlist item
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/wishlist/items/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("product is removed", function () {
    expect(res.status).to.equal(200);
    expect(res.body.data.wishlist.totalItems).to.equal(0);
  });
}
