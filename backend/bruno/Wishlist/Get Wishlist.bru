meta {
  name: Get wishlist
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/wishlist
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("wishlist is returned", function () {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.equal(true);
  });
}
